在当前文件夹下新建一个config.ini文件，并将以下内容copy到该文件中：
[database]
host     = localhost
username = 这里替换为本地数据库的用户名
password = 这里替换为本地数据库的密码
dbname   = 这里替换为本地数据库的名称

[application]
controllersDir = app/controllers/
modelsDir      = app/models/
viewsDir       = app/views/
pluginsDir     = app/plugins/
libraryDir     = app/library/

[cache]
voltCacheDir = cache/volt/

[resource]
#useCDN = 0
baseUri = /

[security]
protectBackend = 1
openHttps = 0
